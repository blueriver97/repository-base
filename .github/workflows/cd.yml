name: CD

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "배포 환경"
        required: true
        type: choice
        options:
          - dev
          - prod

concurrency:
  group: ${{ github.ref }}-${{ github.event.inputs.environment }}-${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read
  checks: read

jobs:
  deploy:
    runs-on: self-hosted
    # 입력받은 environment 값으로 strategy를 설정합니다.
    # develop branch --> only dev, main branch --> dev, prod
    strategy:
      matrix:
        include:
          - branch: develop
            environment: dev
          - branch: main
            environment: dev
          - branch: main
            environment: prod

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Set project name
        run: echo "PROJECT_NAME=$(echo '${{ github.repository }}' | cut -d'/' -f2)" >> "$GITHUB_ENV"

      - name: Prepare server
        # workflow_dispatch의 입력값과 matrix의 environment가 일치할 때만 실행
        if: github.ref == matrix.branch && github.event.inputs.environment == matrix.environment
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ matrix.environment == 'dev' && secrets.DEV_HOST || secrets.PROD_HOST }}
          user: ${{ matrix.environment == 'dev' && secrets.DEV_USER || secrets.PROD_USER }}
          key: ${{ matrix.environment == 'dev' && secrets.DEV_DEPLOY_KEY || secrets.PROD_DEPLOY_KEY }}
          script: |
            sudo mkdir -p /opt/${{ env.PROJECT_NAME }}
            sudo chown -R ${{ matrix.user }}:${{ matrix.user }} /opt/${{ env.PROJECT_NAME }}

      - name: Deploy code
        # workflow_dispatch의 입력값과 matrix의 environment가 일치할 때만 실행
        if: github.ref == matrix.branch && github.event.inputs.environment == matrix.environment
        uses: burnett01/rsync-deployments@7.0.2
        with:
          switches: -az --delete
          path: ./
          remote_path: /opt/${{ env.PROJECT_NAME }}
          remote_host: ${{ matrix.environment == 'dev' && secrets.DEV_HOST || secrets.PROD_HOST }}
          remote_user: ${{ matrix.environment == 'dev' && secrets.DEV_USER || secrets.PROD_USER }}
          remote_key: ${{ matrix.environment == 'dev' && secrets.DEV_DEPLOY_KEY || secrets.PROD_DEPLOY_KEY }}
